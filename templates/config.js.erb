///// @scratch /configuration/config.js/1
 // == Configuration
 // config.js is where you will find the core Grafana configuration. This file contains parameter that
 // must be set before Grafana is run for the first time.
 ///
define(['settings'],
function (Settings) {
  "use strict";

  return new Settings({

    // datasources, you can add multiple
    datasources: {
<%- ds = scope['grafana::_datasources'] -%>
<%- ds.sort.each do |source, source_params| -%>
      <%= source %>: {
<%-   source_params.sort.each do |param, value| -%>
        <%= param %>: <%= value %>,
<%-   end -%>
<%- end -%>
      },
    },

    // elasticsearch url
    // used for storing and loading dashboards, optional
    // For Basic authentication use: http://username:password@domain.com:9200
    elasticsearch: "<%= @config_elasticsearch %>",

    // default start dashboard
    default_route: '<%= @config_default_route %>',

    // timezoneOFfset:
    // If you experiance problems with zoom, it is probably caused by timezone diff between
    // your browser and the graphite-web application. timezoneOffset setting can be used to have Grafana
    // translate absolute time ranges to the graphite-web timezone.
    // Example:
    //   If TIME_ZONE in graphite-web config file local_settings.py is set to America/New_York, then set
    //   timezoneOffset to "-0500" (for UTC - 5 hours)
    // Example:
    //   If TIME_ZONE is set to UTC, set this to "0000"
    //
    timezoneOffset: <%= @config_timezoneOffset %>,

    // Elasticsearch index for storing dashboards
    grafana_index: "<%= @config_grafana_index %>",

    // set to false to disable unsaved changes warning
    unsaved_changes_warning: <%= @config_unsaved_changes_warnings %>,

    // set the default timespan for the playlist feature
    // Example: "1m", "1h"
    playlist_timespan: "<%= @config_playlist_timespan %>",


    // Add your own custom pannels
    plugins: {
      panels: [
<%- @config_panels.each do |cpn| -%>
        '<%= cpn %>',
<%- end -%>
      ]
    }

  });
});
